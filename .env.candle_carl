# Local placeholder env for autonomous Candle Carl runs
# These are safe defaults to avoid startup assertion during diagnostics.
# Replace with real secrets or ensure SOPS-managed values are loaded in production.
HELIUS_API_KEY=placeholder_missing_helius_api_key
KEYPAIR_PATH=/home/dshontaylor/.sops/keypair.json
# Provide KEYPAIR env for backward-compatible wallet loaders (path only, not private key material)
KEYPAIR=/home/dshontaylor/.sops/keypair.json
SOPS_WALLET_FILE=/home/dshontaylor/.sops/wallet.json
TELEGRAM_DISABLED=true

# Autonomous tuning overrides applied by Candle Carl run
# Lower log verbosity to reduce noise in telemetry and avoid frequent restarts
LOG_LEVEL=info
# Increase playbook restart threshold to reduce noisy restart-triggered recoveries (was 12)
PLAYBOOK_RESTART_THRESHOLD=24
PLAYBOOK_RESTART_WINDOW_MS=1800000

# Low-risk performance tuning applied by autonomous run (2026-04-25)
# Reduce external RPC/source pressure and mild concurrency limits to lower memory/RSS spikes
SOURCES_RPS=0.5
PAIR_FETCH_CONCURRENCY=1
# Lower max new entries to reduce processing load
MAX_NEW_ENTRIES_PER_HOUR=2
# Route cache size reduction to lower memory usage
ROUTE_CACHE_MAX_SIZE=256
# Tighten momentum entry thresholds to reduce low-quality entries (Candle Carl 2026-04-24)
PAPER_ENTRY_RET_15M_PCT=0.02
PAPER_ENTRY_RET_5M_PCT=0.005
BIRDEYE_LITE_ENABLED=false
# Adjustments from autonomous optimization cycle (2026-04-25)
# Increase route cache TTL to reduce frequent route lookups that lead to snapshot failures
ROUTE_CACHE_TTL_MS=120000
# Increase route cache max size slightly to reduce eviction churn
ROUTE_CACHE_MAX_SIZE=512
# Allow slightly older snapshots to be accepted to tolerate brief provider delays
CONFIRM_MAX_SNAPSHOT_AGE_MS=12000
# Reduce debug canary verbosity to limit expensive diagnostics
DEBUG_CANARY_VERBOSE=false
# Conservative scan cadence change applied by autonomous run (2026-04-26)
# Increase scan interval to reduce CPU and RPC pressure (low-risk)
SCAN_EVERY_MS=900000

