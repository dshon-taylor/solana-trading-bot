2026-04-23T09:03Z — Autonomous Candle Carl run
- Diagnostics: pm2 shows solana-momentum-bot online; restarts historically high (535). Logs show repeated Telegram 404 errors but TELEGRAM_DISABLED=true and TELEGRAM_POLL_ENABLED=false so polling disabled.
- Observed memory RSS spikes up to ~1.1GB at 08:50; p95 event loop latency peaked ~375ms earlier. Active runners=0; entries/hour ~0.
- Changes applied (low-risk):
  1) ecosystem.config.cjs: max_memory_restart 4096M -> 2048M
  2) ecosystem.config.cjs: BIRDEYE_WS_MAX_SUBS 2 -> 1
  3) ecosystem.config.cjs: WATCHLIST_EVAL_EVERY_MS 300000 -> 600000
- Rationale: reduce memory pressure, FD usage, and polling frequency to improve stability. Changes committed and pushed (branch tune/candle-carl-autotune-2026-04-22, commit 8a324cf).
- Restart: pm2 restart solana-momentum-bot --update-env succeeded; process online. PID and uptime refreshed.
- Env checks: TELEGRAM_DISABLED present; KEYPAIR path present; SOLANA RPC endpoints set in .env. pm2 env shows some old env values — pm2 may be reading environment caching; recommend verifying effective runtime env variables after next scheduled restart or consider pm2 delete + start during maintenance window if values don't update.
- No revert scheduled. Will monitor; per policy, auto-revert if metrics worsen for 2 consecutive runs.
