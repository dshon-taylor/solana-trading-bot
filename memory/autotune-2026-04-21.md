2026-04-21T14:00Z - Autonomous Candle Carl run (D'Shon)
- Action: low-risk change: appended NODE_OPTIONS=--max-old-space-size=512 to trading-bot/.env to cap Node memory (autotune).
- Rationale: historical pm2 restarts and occasional memory growth; cap is conservative.
- Result: pm2 restart applied; solana-momentum-bot restarted and reported status "online". Interpreter args still show --max-old-space-size=1536 (pm2 interpreter arg), verify next run. Monitor for metric regressions; auto-revert if two consecutive degraded runs.
- Commit: 6acc2d7 (autotune: set NODE_OPTIONS max-old-space-size=512)
- Notes: no code changes; only env file added (was previously .gitignored). Will review pm2 interpreter args and adjust if needed in a future run.
