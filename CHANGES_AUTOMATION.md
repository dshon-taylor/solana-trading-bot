2026-04-25 — Candle Carl automation run
- Added startup env sanity log and global error handlers to improve robustness and reduce unhandled rejections causing crashes.
- Branch: tune/candle-carl-2026-04-23 (local commit 5e571ea)
- Restart: pm2 restart solana-momentum-bot --update-env (succeeded)

Notes:
- No remote push performed (origin remote missing). If you want these changes pushed to remote, add the remote and push or tell automation to open a PR.